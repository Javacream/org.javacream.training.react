{
  "name": "people_app",
  "version": "1.0.0",
  "author": "Javacream",
  "scripts": {
    "go": "open http://localhost:4000; npm run server",
    "e2e": "nightwatch",
    "test": "./node_modules/.bin/concurrently -k 'npm run server' 'npm run e2e'",
    "start": "npm run server",
    "server": "node ./node_modules/live-server/live-server.js public --host=localhost --port=4000 --middleware=./disable-browser-cache.js"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "2.2.0",
    "live-server": "git://github.com/acco/live-server.git"
  }
}
